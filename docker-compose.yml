services:
  web:
    build: .
    image: descargador-dian-web:local
    ports:
      - "8001:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - dian_jobs:/data/dian-jobs

volumes:
  dian_jobs:
